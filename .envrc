KERN_JAVA_HOME="$(brew --prefix openjdk@25)/libexec/openjdk.jdk/Contents/Home"

if [ ! -x "$KERN_JAVA_HOME/bin/java" ]; then
  echo "Kern requires Homebrew openjdk@25. Install it with: brew install openjdk@25" >&2
  exit 1
fi

export JAVA_HOME="$KERN_JAVA_HOME"
PATH_add "$JAVA_HOME/bin"
